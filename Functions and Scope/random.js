(function(m,n){
    n=Math.floor.random.(m,n)
    console.log(n)
}
   
(2,8))
// (function(min, max) {
//     const randomNumber = Math.floor(Math.random() * (max - min + 1)) + min;
//     console.log(`Random number between ${min} and ${max}: ${randomNumber}`);
// })(1, 10); // Output: Random number between 1 and 10: 7 (example)
